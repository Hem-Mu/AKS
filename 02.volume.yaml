apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-hamtorage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: hamtorageclass
  # SC name
  resources:
    requests:
      storage: 5Gi
